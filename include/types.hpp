#pragma once

namespace symbo {
	/**
	 * A function f(x)
	 *
	 * For example: sin(x) is a function
	 */
	class Function {
	public:
	private:
	};

	/**
	 * A term in an equation
	 *
	 * For example: x^2 + 3x + 2 has terms
	 * 	-> x^2
	 * 	-> 3x
	 * 	-> 2
	 */
	class Term {
	public:
	private:
	};
} // namespace symbo